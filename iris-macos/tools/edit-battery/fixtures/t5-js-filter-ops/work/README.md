# filter-dsl

The little filter language behind saved views in the reporting UI.

## Build

    npm run build

## Test

    npm test

The operator contract every operator must satisfy is written out in
`docs/FILTERS.md`. Read that before adding one.
