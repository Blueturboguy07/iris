# licensekit

Offline licence verification for the desktop app. No network at runtime:
the app ships with the vendor's public keys and checks signatures locally.

## Build

    npm run build

## Test

    npm test

See `docs/LICENSING.md` for how licences and keys work, and `CHANGELOG.md`
for what has changed.
