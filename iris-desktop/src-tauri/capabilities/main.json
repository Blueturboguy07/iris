{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "iris-main",
  "description": "Minimal IPC surface for the local Iris overlay.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging"
  ]
}
