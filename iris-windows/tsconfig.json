{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "moduleResolution": "node",
    "lib": ["ES2022"],
    "types": ["node"],
    "outDir": "dist",
    "rootDir": ".",
    "strict": true,
    "noUncheckedIndexedAccess": false,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "sourceMap": true
  },
  "//": "The checking config: covers the app AND the suite, so `npm run typecheck` sees both. Emitting is done by tsconfig.build.json.",
  "//rootDir": "\".\", not \"..\". It was widened only so tests/maintain-break-signature.test.ts could import publik's normalizer across the repository boundary; that import is gone (it broke when the clients were split out, and the check is golden vectors now), so nothing in this project may reach outside it again.",
  "include": ["src/**/*", "tests/**/*", "forge.config.ts", "vitest.config.ts"],
  "exclude": ["node_modules", "dist", "out"]
}
