{
  "name": "iris",
  "private": true,
  "description": "Iris \u2014 the publik desktop assistant. Three clients; this root holds only the cross-client checks.",
  "scripts": {
    "test": "vitest run",
    "iris:deploy:macos": "scripts/deploy-iris-local.sh",
    "iris:build:windows": "cd iris-windows && npm run make",
    "tauri:dev": "cd iris-desktop && pnpm tauri dev",
    "tauri:build:macos": "scripts/build-iris-macos.sh"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.10"
  },
  "packageManager": "pnpm@10.0.0"
}
