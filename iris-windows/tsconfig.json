{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "moduleResolution": "node",
    "lib": ["ES2022"],
    "types": ["node"],
    "outDir": "dist",
    "rootDir": "..",
    "baseUrl": ".",
    "paths": { "@iris/core": ["../packages/iris-core/src/index.ts"] },
    "strict": true,
    "noUncheckedIndexedAccess": false,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "sourceMap": true
  },
  "//": "The checking config: covers the app AND the suite, so `npm run typecheck` sees both. Emitting is done by tsconfig.build.json.",
  "//rootDir": "\"..\" (the iris repo root), not \".\", because src/ and tests/ both import @iris/core, which lives in ../packages/iris-core. This is check-only: tsconfig.build.json sets its own narrower rootDir and excludes tests/, so widening this file's rootDir has no effect on what ships.",
  "include": ["src/**/*", "tests/**/*", "forge.config.ts", "vitest.config.ts"],
  "exclude": ["node_modules", "dist", "out"]
}
